define("store", function (store) {
    return function () {
        var searchHistory = [];
        store.subscribe(function () {
            var state = store.getState();
        });
        return {

        };
    };
});